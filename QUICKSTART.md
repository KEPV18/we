# 🚀 دليل البدء السريع

## خطوة 1️⃣: احصل على Bot Token

1. افتح تيليجرام وابحث عن [@BotFather](https://t.me/BotFather)
2. ابعت `/newbot`
3. اتبع التعليمات واختر اسم للبوت
4. احفظ الـ Token (مثال: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## خطوة 2️⃣: رفع المشروع على GitHub

```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit - WE Usage Bot"
git branch -M main

# أنشئ repository على GitHub ثم:
git remote add origin https://github.com/YOUR_USERNAME/we-usage-bot.git
git push -u origin main
```

## خطوة 3️⃣: النشر على Render

### أ) إنشاء Web Service

1. اذهب إلى [render.com](https://render.com)
2. سجّل دخول بـ GitHub
3. اضغط **"New +"** → **"Web Service"**
4. اختر الـ Repository الخاص بك
5. املأ البيانات:
   - **Name**: `we-usage-bot` (أو أي اسم)
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Instance Type**: `Free`

### ب) إضافة Environment Variables

في صفحة الخدمة، اذهب لـ **"Environment"** وأضف:

| المتغير | القيمة |
|---------|--------|
| `BOT_TOKEN` | التوكن من BotFather |
| `NODE_ENV` | `production` |

### ج) Deploy

1. اضغط **"Create Web Service"**
2. انتظر 5-10 دقائق حتى ينتهي البناء
3. بعد النجاح، ستحصل على URL مثل:  
   `https://we-usage-bot-xxxx.onrender.com`

### د) تعيين Webhook URL

1. ارجع لـ **"Environment"** في Render
2. أضف متغير جديد:
   - **Key**: `RENDER_EXTERNAL_URL`
   - **Value**: الـ URL الذي حصلت عليه (من الخطوة السابقة)
3. احفظ → سيُعاد تشغيل الخدمة تلقائياً

## خطوة 4️⃣: اختبار البوت

1. افتح Telegram
2. ابحث عن البوت باسمه
3. ابعت `/start`
4. إذا رد عليك → **مبروك! البوت شغال** 🎉

## خطوة 5️⃣: ربط حساب WE

1. ابعت `/link` للبوت
2. ابعت رقم الخدمة (Service Number)
3. ابعت الباسورد
4. انتظر... سيتم ربط الحساب
5. ابعت `/status` لمشاهدة استهلاكك

---

## المشاكل الشائعة

### ❌ البوت مش بيرد
- تأكد من `BOT_TOKEN` صحيح
- تأكد من `RENDER_EXTERNAL_URL` مضبوط بالظبط

### ❌ "Playwright not installed"
- Render بيثبته تلقائياً في `postinstall`
- انتظر البناء يخلص كامل

### ⏰ البوت بطيء
- Render Free Plan بينام بعد 15 دقيقة
- **الحل:** استخدم [UptimeRobot](https://uptimerobot.com) للـ ping كل 10 دقائق

---

## 🎯 الأوامر الأساسية

| الأمر | الوصف |
|------|--------|
| `/start` | البدء |
| `/link` | ربط حساب WE |
| `/status` | عرض الحالة |
| `/today` | استهلاك اليوم |
| `/renew` | التجديد |
| `/logout` | تسجيل الخروج |

---

**🔥 للمزيد من التفاصيل:** اقرأ [DEPLOY.md](DEPLOY.md)
